#include "igl.hpp"
#include "object.hpp"
#include "light.hpp"


int main(int argc, char** argv)
{
  /*vector<Object> obj;
  
  obj.push_back(Object());
  
  for (unsigned int i=0; i < obj.size(); i++)
    obj[i].display();
  
  obj[0].addProperties(Light(0.0, 0.0, 1., 0.0));
  
  
  for (unsigned int i=0; i < obj.size(); i++)
    obj[i].display();
  
  obj.push_back(Object());
  
  for (unsigned int i=0; i < obj.size(); i++)
    obj[i].display();
  
  obj[1].addProperties(Light());
  
  for (unsigned int i=0; i < obj.size(); i++)
    obj[i].display();
  
  obj.clear();
  for (unsigned int i=0; i < obj.size(); i++)
    obj[i].display();*/
}